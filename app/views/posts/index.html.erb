<h1>This is the INDEX page inside POSTS folder. It shows all the posts.</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Title</th>
      <th>Published</th>
    </tr>
  </thead>
  
  <tbody>
    <% @posts.each do |whatever| %>
    <tr>
      <td><%= whatever.id %></td>
      <td><a href="/posts/<%= whatever.id %>"><%= whatever.title %></a></td>
      <td><%= whatever.created_at %></td>
    </tr>
    <% end %>
  </tbody>
</table>